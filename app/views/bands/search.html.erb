<div class="row">
  <% @bands.each do |band| %>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
        <%= link_to (image_tag band.image.url(:medium), class: 'image'), band %>
      </div>
    </div>
  <% end %>
</div>